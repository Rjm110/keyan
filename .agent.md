# Agent 开发规范（cubepi 科研助手）

## 开发手册要求（强制）

每次进行开发（实现功能、修改代码、修复 bug、重构等），都必须把**本次开发的过程和修改（新增）的代码**写进开发手册文件夹：

- 路径：`/home/lmz/下载/cubepi-main/开发手册/`
- 每个开发任务一个 Markdown 文件，命名格式：**"这次开发的内容 + 时间"**，例如：
  - `2026-08-31-搭建项目骨架.md`
  - `2026-08-31-实现文件系统工具集.md`
- 文件内容必须包含：
  1. **开发目标**：本次要做什么
  2. **开发过程**：关键决策、遇到的问题、解决方式
  3. **新增/修改的代码**：列出每个文件，附上关键代码片段（完整新增文件可附全文，修改文件附 diff 要点）
  4. **验证方式**：如何验证本次开发正确（测试命令、手动验证步骤）
  5. **下一步**：后续待办

**时机**：每次开发任务完成后立即写，不要攒到最后。如果一次会话有多个开发任务，每个任务单独一个 md 文件。

## 项目约定（继承自 AGENTS.md）

- 命令一律用 `uv`：`uv sync --all-extras --dev`、`uv run pytest`、`uv run ruff check`、`uv run ruff format --check`、`uv run mypy`
- 测试用 `FauxProvider` 确定性驱动，`asyncio_mode=auto`
- 代码风格：ruff + mypy 严格模式，`from __future__ import annotations`，类型注解齐全
- 应用代码放 `app/` 目录（科研助手应用），不修改 cubepi 库本身